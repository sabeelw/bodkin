# BodkinBuyOnce

```sh
# foundry: https://book.getfoundry.sh/getting-started/installation
forge install foundry-rs/forge-std --no-git
forge test
forge build
```

Then `bodkin helper deploy --live` and set `HELPER_ADDRESS`.
